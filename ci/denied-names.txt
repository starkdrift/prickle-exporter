# Discarded candidate names — one per line, blank lines and #-comments ignored.
#
# SPEC.md §Identity: the names in the identity table are the only ones used
# anywhere in the tree. Every name considered and rejected during naming goes
# here, and ci/check.sh greps the working tree for it. The grep excludes this
# file and SPEC.md, which are the two places a discarded name may legitimately
# be written down.
#
# Matching is case-insensitive and substring-based, so a short entry will catch
# a lot: prefer the full discarded name.
#
# THIS LIST IS EMPTY, AND THAT NO LONGER MEANS THE TREE IS UNPROTECTED.
#
# The names discarded during the naming pass were never recorded anywhere in
# the repository, so they could not be recovered when this file was created and
# still cannot be. For as long as that is true this list can only ever be
# filled from somebody's memory.
#
# So ci/check.sh does not depend on it. Its naming-discipline step asserts
# positively that the only exporter-shaped identifiers in the tree are
# prickle-exporter and the third-party exporters the docs name — which catches
# a discarded candidate resurfacing without anyone having to remember what it
# was, and catches a brand-new unagreed name too. Adding entries here layers a
# second, narrower check on top; it does not switch the first one on.
